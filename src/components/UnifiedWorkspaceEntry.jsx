import React from "react";
import UnifiedWorkspace from "./UnifiedWorkspace";

export default function UnifiedWorkspaceEntry() {
  return (
    <section style={{ width: "100%", minHeight: "100vh" }}>
      <UnifiedWorkspace />
    </section>
  );
}

